export interface Position {
    latitude_deg: number;
    longitude_deg: number;
    altitude_m: number;
}

export const DefaultPosition =
{
    latitude_deg : 0,
    longitude_deg : 0,
    altitude_m : 0
}